#include "main.h"
/*
 * this code checks for uppercase
 *  
 *  return 1 if success return 0 on failure
 */
int _isupper(int c);
{
	if ( c >= 'A' && c <= 'Z')
	{
	
		return (1);
	}
	{
	else 
		return (0);
}
}
